type User {
    id: ID!
    name: String!
    handle: String!
    email: String!
    password: String
}

extend type Query {
    findAllUsers: [User]!
    countUsers: Long!
}

extend type Mutation {
    newUser(name: String!, handle: String!, email: String!, password: String!) : User!
}
